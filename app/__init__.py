"""Top-level app package.

The FastAPI application (REST API + HTML frontend) is built by
``app.web:create_app``. Database utilities and models live in ``app.db``
and ``app.models.catalog`` respectively.
"""
